# Default Logic behaviour that the AI will fallback on if they are in a civil war
# Should focus on getting the nation united again no matter their ideology
# Written by Henrik "Groogy" Hansson

NOS_civilwar_peace = {
	# ROOT = Taker, FROM = Giver
	enable = {
		OR = {
			AND = {
				ROOT = { OR = { original_tag = SPR original_tag = SPS } }
				FROM = { OR = { original_tag = SPR original_tag = SPS } }
			}
			AND = {
				ROOT = { OR = { original_tag = RUS original_tag = SOV } }
				FROM = { OR = { original_tag = RUS original_tag = SOV } }
			}
			AND = {
				ROOT = { OR = { original_tag = GER original_tag = WGR original_tag = DDR } }
				FROM = { OR = { original_tag = GER original_tag = WGR original_tag = DDR } }
			}
			AND = {
				ROOT = { OR = { original_tag = RUS original_tag = FRA } }
				FROM = { OR = { original_tag = RUS original_tag = VCF } }
			}
			AND = {
				ROOT = { OR = { original_tag = USA original_tag = CSA original_tag = FAS original_tag = ACO } }
				FROM = { OR = { original_tag = USA original_tag = CSA original_tag = FAS original_tag = ACO } }
			}
		}
	}

	annex_randomness = 0
	liberate_randomness = 0
	puppet_randomness = 0
	take_states_randomness = 0
	force_government_randomness = 0
	
	# ROOT = Taker, FROM = Giver
	annex = {
		factor = 1000
	}

	# ROOT = Taker, FROM = Liberated
	liberate = {
		factor = 0
	}

	# ROOT = Taker, FROM = Giver
	puppet = {
		factor = 0
	}

	# ROOT = Taker, FROM = Giver
	puppet_all = {
		base = 0 
	}
	
	# ROOT = STATE, FROM = Taker, FROM.FROM = Giver
	puppet_state = {
		base = 0 
	}
	
	# ROOT = Taker, FROM = State
	take_states = {
		factor = 0
	}

	# ROOT = Taker, FROM = Giver
	force_government = {
		factor = 0
	}
}
